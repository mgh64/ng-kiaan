//
// schemas for working on data in mongo.
// these schemas are a good filters to prevent inserting bad data to DB,
// and if the data fields not exists in these files, that field didn't
// save in DB.
//
// and all other files of app user export of this file for their db works.


// note : in DB I didn't save any false value for Boolean fileds, 2 thing
// is possible : Boolean value is true and I know it's true else it's
// not exists in object and it means it's false.


// return each schema file for use in other part of app.
module.exports = (function() {
   var _return = {};

   [
      'permissions',
      'g_users',
      'users',
      'g_persons',
      'persons',
      'banks',
      'branches',
      'g_products',
      'products',
      'units',
      'costs',
      'incomes',
      'bys',
      'operations',
      'invoices',
      'rps',
      'bankaccounts'

   ].forEach(function(schema_name) {
      _return[schema_name] = require('./schemas/' + schema_name);
   });

   return _return;
})();
