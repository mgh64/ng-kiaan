//
//
//

var mongoose = require('mongoose');
var Schema = mongoose.Schema;
var ObjectId = Schema.Types.ObjectId;

var bank_schema = Schema({
  //
  id : Number,
  title: String,
  creator_id: {
    type: ObjectId,
    ref: 'users'
  }
});


module.exports = mongoose.model('banks', bank_schema);
