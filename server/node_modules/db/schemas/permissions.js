//
//
//

var mongoose = require('mongoose');
var Schema = mongoose.Schema;
var ObjectId = Schema.Types.ObjectId;



var permissions_schema = Schema({
    title: {String},
    name: {
      type: String,
      lowercase: true,
      trim: true
    }
});

module.exports = mongoose.model('permissions', permissions_schema);
